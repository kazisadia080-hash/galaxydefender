## Run

```powershell
cd GalaxyDefender
mvn package
java -jar target/galaxy-defender-1.0.0.jar
```

Controls: `WASD` move, `1` normal auto-fire, `2` rapid auto-fire, `3` turbo double-shot, `P` pause, `Esc` exit. Shooting is automatic, so focus on lining up the ship with enemies.