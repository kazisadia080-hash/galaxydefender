package com.galaxydefender.util;
/** Sound hook. Resource audio can be added later. */
public final class SoundPlayer { private SoundPlayer(){} public static void play(String resource){ /* intentionally optional: game runs without audio assets */ } }
